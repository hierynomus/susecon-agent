apiVersion: v1
kind: Service
metadata:
  name: {{ include "susecon-agent.fullname" . }}
  labels:
    {{- include "susecon-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "susecon-agent.selectorLabels" . | nindent 4 }}
